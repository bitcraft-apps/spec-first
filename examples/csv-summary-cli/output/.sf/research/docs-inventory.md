# Documentation Inventory

## Existing Docs

| File | Primary topic |
|------|---------------|
| (none) | The project has no documentation files |

## Recommendation

No file fits the change, so create one. `docs.md` at the project root holds the user
documentation: install, the commands, the output format and the known limits.

The technical reference stays in `.sf/research/technical-docs.md`. The public surface is
one command and three functions, which `docs.md` covers. A second file would duplicate it.
