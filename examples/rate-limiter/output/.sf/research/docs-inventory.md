# Documentation Inventory

## Existing Docs

| File | Primary topic |
|------|---------------|
| (none) | The project has no documentation files |

## Recommendation

No file fits the change, so create one. `docs.md` at the project root holds the setup, the
two entry points and the limits a caller must know.

The public surface is one class, one interface and one middleware factory. One file covers
both the user and the developer. A second file would repeat the configuration table.
