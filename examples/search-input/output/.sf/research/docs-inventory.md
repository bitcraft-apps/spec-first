# Documentation Inventory

## Existing Docs

| File | Primary topic |
|------|---------------|
| (none) | The project has no documentation files |

## Recommendation

No file fits the change, so create one. `docs.md` at the project root holds the props
table, one usage example and the behavior a caller must know.

The component has five props and no runtime configuration. One file covers both the user
and the developer. A second file would repeat the props table.
